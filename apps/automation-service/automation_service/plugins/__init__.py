"""Plugin package — master prompt section 53.

Plugins live under ``/home/z/my-project/plugins/`` and are discovered by
the :class:`~automation_service.plugins.manager.PluginManager`.

This ``__init__`` module is intentionally empty: it only marks the
directory as a Python package so ``from automation_service.plugins import
...`` works.
"""

from __future__ import annotations
